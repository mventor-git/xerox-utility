# HANDOVER
- Completed: bootstrap→run_once + single-box verify + git/GitHub + guide + setup v2 (proven e2e) + polished GUI + hardcode audit + retrieve exhaustion log.
- Current: tickets 015+016 completed. Next: downloader browser-flow (Playwright playbook ready), then delete UI wiring.
- Notes: IP `192.168.1.20` is config default, never hardcode; legacy UTF-16 INI ignored; retrieve blob 503s direct — don't retry blindly, drive browser flow.
- Warnings: never create folders/users on device; delete only on confirm; don't edit `profile.md`/`.muse`.
